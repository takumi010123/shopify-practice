<section class="products">
  <h2 class="products__heading">{{ section.settings.heading }}</h2>
  <div class="product-grid">
    {% for product in section.settings.collection.products %}
      {% render 'product-card', product: product %}
    {% endfor %}
  </div>
</section>

{% schema %}
  {
    "name": "おすすめ商品",
    "settings": [
      {
        "type": "collection",
        "id": "collection",
        "label": "表示するコレクション"
      },
      {
        "type": "text",
        "id": "heading",
        "label": "見出し",
        "default": "おすすめの豆"
      }
    ]
  }
{% endschema %}